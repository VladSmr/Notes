<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.2</version>
        <relativePath/>
    </parent>

    <groupId>ru.importer</groupId>
    <artifactId>notes</artifactId>
    <version>1.0.6</version>
    <name>notes</name>
    <description>notes</description>

    <properties>
        <java.version>21</java.version>
        <selenium.version>4.21.0</selenium.version>
        <javafx.version>21.0.2</javafx.version>
        <javafx.platform>win</javafx.platform>
        <skipTests>true</skipTests>
        <excludedGroups>integration</excludedGroups>
        <jpackage.type>msi</jpackage.type>
        <jpackage.name>KP IMDB Importer</jpackage.name>
        <jpackage.vendor>KP IMDB Importer</jpackage.vendor>
        <jpackage.appVersion>${project.version}</jpackage.appVersion>
        <jsoup.version>1.18.1</jsoup.version>
        <bonigarcia.version>5.9.2</bonigarcia.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>${bonigarcia.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>${jsoup.version}</version>
        </dependency>
        <!-- JavaFX (WebView-оболочка для десктоп-окна).
             Все модули объявляем ЯВНО с classifier win (нативные DLL для Windows).
             Транзитивные org.openjfx-зависимости исключаем (wildcard), потому что в их
             POM стоит <classifier>${javafx.platform}</classifier>, который в этой сборке
             не раскрывается (OS-профиль родительского POM org.openjfx:javafx не
             активируется при транзитивной резолюции) — иначе Maven пытается качать
             jar с буквальным именем "${javafx.platform}". Мы сами даём все модули. -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
            <version>${javafx.version}</version>
            <classifier>win</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
            <classifier>win</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
            <classifier>win</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>${javafx.version}</version>
            <classifier>win</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-web</artifactId>
            <version>${javafx.version}</version>
            <classifier>win</classifier>
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.seleniumhq.selenium</groupId>
                <artifactId>selenium-bom</artifactId>
                <version>${selenium.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>${skipTests}</skipTests>
                    <excludedGroups>${excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Профили сборки десктоп-установщика через jpackage (входит в JDK 21).

              mvnw clean package -DskipTests=false               # fat-JAR (для dev)
              mvnw package -Pdesktop -DskipTests=false           # app-image (.exe, без WiX)
              mvnw package -Pdesktop-msi -DskipTests=false       # .msi (нужен WiX 3.x)

            Оба desktop-профиля делегируют фактическую сборку в build-msi.ps1
            (тонкий jar + app/*, jlink runtime-image с JavaFX, jpackage). Скрипт
            вызывается с -SkipBuild, т.к. Maven уже собрал thin jar к фазе package.

            Требования:
              - JDK 21 (jpackage, jlink);
              - JavaFX 21.0.2 win-jar в локальном m2 (уже объявлены в <dependencies>);
              - иконка packaging/app.ico (валидный .ico; если это PNG — перегенерируется);
              - для .msi — WiX Toolset 3.x (без него только app-image).

            Параметры (переопределяются через -D):
              jpackage.name, jpackage.vendor, jpackage.appVersion
        -->

        <!-- app-image: промежуточная папка target/dist/KP IMDB Importer/ (без WiX). -->
        <profile>
            <id>desktop</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>powershell</executable>
                                    <arguments>
                                        <argument>-ExecutionPolicy</argument>
                                        <argument>Bypass</argument>
                                        <argument>-File</argument>
                                        <argument>${project.basedir}/build-msi.ps1</argument>
                                        <argument>-SkipBuild</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- msi: установщик (нужен WiX 3.x). Включает ярлыки в меню Пуск и на рабочем столе. -->
        <profile>
            <id>desktop-msi</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-msi</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>powershell</executable>
                                    <arguments>
                                        <argument>-ExecutionPolicy</argument>
                                        <argument>Bypass</argument>
                                        <argument>-File</argument>
                                        <argument>${project.basedir}/build-msi.ps1</argument>
                                        <argument>-SkipBuild</argument>
                                        <argument>-Msi</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
